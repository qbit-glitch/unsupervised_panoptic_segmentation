# Leave-one-out: disable G3 LSJ augmentation only, keep rest of M2 stack.
_BASE_: "M2_stacked_novel.yaml"

AUGMENTATION:
  LSJ:
    ENABLED: False

TRAINING:
  STEPS: 15000       # half schedule for LOO sweep

SYSTEM:
  RUN_NAME: "LOO_noG3"
  LOG_PATH: "results/stage2_m2f/LOO_noG3"
