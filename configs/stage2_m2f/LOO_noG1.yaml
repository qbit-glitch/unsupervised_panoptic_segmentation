# Leave-one-out: disable G1 EMA only, keep rest of M2 stack.
_BASE_: "M2_stacked_novel.yaml"

MODEL:
  EMA:
    ENABLED: False

TRAINING:
  STEPS: 15000       # half schedule for LOO sweep

SYSTEM:
  RUN_NAME: "LOO_noG1"
  LOG_PATH: "results/stage2_m2f/LOO_noG1"
