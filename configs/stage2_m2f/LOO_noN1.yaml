# Leave-one-out: revert N1 decoupled -> standard query pool.
_BASE_: "M2_stacked_novel.yaml"

MODEL:
  MASK2FORMER:
    QUERY_POOL: "standard"       # revert to M1 default
    NUM_QUERIES: 100

TRAINING:
  STEPS: 15000       # half schedule for LOO sweep

SYSTEM:
  RUN_NAME: "LOO_noN1"
  LOG_PATH: "results/stage2_m2f/LOO_noN1"
