# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
scipy>=1.10.0
Pillow>=9.5.0
tqdm>=4.65.0
PyYAML>=6.0

# HuggingFace (for DA2, DepthPro)
transformers>=4.35.0

# Depth Anything V3 (custom package — pin commit or version)
# NOTE: depth_anything_3 is NOT on PyPI. Install from source:
#   pip install git+https://github.com/.../depth_anything_3.git@COMMIT_HASH
# depth-anything-3  # uncomment after installing from source

# For validation metrics
scikit-learn>=1.3.0
