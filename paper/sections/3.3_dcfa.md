---
section: "3.3 Depth-Conditioned Feature Adapter"
status: locked v5 (2026-04-29)
audit_score: 8.7/10 (third-audit) + 4 final copy-edits applied
audit_history:
  - v3 first audit 9.5/10 (named-terms pass)
  - v4 fixed six technical errors flagged in user review (depth-only architecture, preservation averaging, etc.)
  - v5 third audit 8.7 → ~9.2 with four final copy-edits (re-shaping → nudging; small MLP wording; pair set $\mathcal{P}$; same-depth sentence dropped)
paragraphs: 5
equations: 3 (Eq. 2 sinusoidal embedding, Eq. 3 residual update, Eq. 4 self-supervised loss with $\mathcal{L}_\text{depth}$ + $\mathcal{L}_\text{preserve}$ sub-terms)
words: ~720
hyperparameters_in_prose:
  - $h = 384$ (with why-clause)
  - 16-D sinusoidal embedding
  - ~225K trainable parameters (225,114; concat input $A: \mathbb{R}^{106} \to \mathbb{R}^{90}$, $h=384$, two hidden layers + two LayerNorms; verified 2026-06-26 from the deployed checkpoint results/depth_adapter/V3_dd16_h384_l2/best.pt, mlp.0.weight = [384,106])
  - $|\mathcal{U}| = 1024$ sampled pixels per minibatch image
  - $P = 1024$ pixel pairs (with stochastic-estimator-scale why-clause)
  - $\sigma_d = 0.5$ (depth-similarity bandwidth)
  - $\lambda_\text{preserve} = 20$ (with high-similarity-neighbourhood why-clause)
architecture_note: "Adapter is CONCAT input: the 90-D code $z_u$ is concatenated with the 16-D sinusoidal depth encoding $e(d_u)$ into a 106-D input ($A: \\mathbb{R}^{106} \\to \\mathbb{R}^{90}$, two 384-wide hidden layers each with LayerNorm + ReLU, zero-init output). 225,114 params. Verified from the deployed checkpoint V3_dd16_h384_l2/best.pt (mlp.0.weight = [384,106]). The earlier depth-only re-spec was incorrect and was reversed 2026-06-26 with user go-ahead. Two pixels at the same depth do NOT receive the same shift, since the shift also depends on the code $z_u$."
citations:
  - "[Sick et al., 2024] — DepthG, depth-guided correlation principle"
bridges:
  from: "§3.2 closing pointer (semantic-branch failure modes: illumination split, appearance merge)"
  to: "§3.4 SIMCF (output-level disagreement, three-cue agreement filter)"
  internal:
    - "depth signal" / "conditioning on $d_u$" (¶1→¶2)
    - "encoding $e(d_u)$" (¶2→¶3)
    - "Training the adapter" (¶3→¶4)
    - "Even with depth-conditioned semantic codes" (¶4→¶5)
notes:
  - DepthPro [Bochkovskii et al., 2024] is committed in §3.2 v5 as the depth model used in main detector results; §3.3 inherits this commitment by using depth value $d_u$ from $D$ without re-stating the depth model.
  - DCFA citation policy: cite DepthG once, attribute the depth-feature correlation principle, distinguish the architectural difference (adapter-level vs end-to-end) as a consequence.
  - Loss sub-terms named with their original-author terminology — $\mathcal{L}_\text{depth}$ in the sense of DepthG, $\mathcal{L}_\text{preserve}$ as our manifold-anchor — with under-brace labels in Eq. 4.
v3_v4_corrections_summary:
  - "[REVERTED 2026-06-26, user go-ahead] v4 had changed concat to depth-only; that was WRONG. The deployed adapter (V3_dd16_h384_l2/best.pt) is concat $A: \\mathbb{R}^{106} \\to \\mathbb{R}^{90}$ with 225,114 params, verified directly from the checkpoint (mlp.0.weight = [384,106]). Restored to concat with the correct param count; the v4 ~76K and ~41K figures were both wrong."
  - "Preservation term in Eq. 4 properly averaged over sampled pixel set $\\mathcal{U}$"
  - "'Both are settled' softened to 'Both expose cases where the depth signal can disambiguate'"
  - "'feature-similarity cue of SIMCF-A' corrected to 'feature-similarity cue used in SIMCF' (SIMCF-A is semantic uniformity, not feature similarity)"
  - "'single physical surface ends up as a single cluster' softened to 'encourages pixels on the same physical surface to remain clustered'"
---

# 3.3 Depth-Conditioned Feature Adapter

The semantic regions inherited from the frozen CAUSE-TR code are appearance-driven, and §3.2 closed by naming the two failure modes this produces inside $G$: a single physical surface is split across an illumination change, such as sun and shade on the same road, and two adjacent same-class objects whose appearance differs only in fine geometry are merged into one. Both expose cases where the depth signal that already governs the instance branch can disambiguate appearance-driven semantic errors, but only if the semantic code can be conditioned on $D$ without retraining the backbone that the rest of the pipeline depends on. We therefore introduce a small residual adapter that reads the frozen 90-dimensional code $z_u$ at pixel $u$ together with the depth value $d_u$ and adds a learned shift to that code, nudging cluster boundaries toward depth-consistent regions where appearance fails, while preserving the unsupervised prior elsewhere.

Conditioning on $d_u$ requires a representation rich enough for a small MLP to read both fine boundary differences and coarse depth ordering. Since $d_u \in [0, 1]$ is a scalar with low dynamic range, we lift it through a 16-dimensional sinusoidal positional encoding with eight exponentially-spaced frequencies $\omega_k = 2^k \pi$ for $k = 0, \dots, 7$,

$$
e(d_u) = \big[\sin(\omega_0 d_u),\, \cos(\omega_0 d_u),\, \ldots,\, \sin(\omega_7 d_u),\, \cos(\omega_7 d_u)\big] \in \mathbb{R}^{16},
\tag{2}
$$

so that the high-frequency channels make boundary-scale depth differences linearly readable while the low-frequency channels expose foreground-versus-background ordering. This mirrors the role of positional encodings in transformers, applied to a one-dimensional metric input rather than to a sequence position.

The encoding $e(d_u)$ is concatenated with the frozen code $z_u$ into a 106-dimensional input, which a three-layer MLP $A: \mathbb{R}^{106} \to \mathbb{R}^{90}$ maps to a residual shift. Each of the first two layers is a linear map to width $h = 384$ followed by layer normalization and a ReLU, and the third projects to the 90-dimensional output and is zero-initialized, so that the adapter reads both the code and its depth context before deciding how to move it,

$$
\tilde z_u = z_u + A\big([\,z_u,\; e(d_u)\,]\big),
\tag{3}
$$

where $[\,\cdot,\cdot\,]$ denotes concatenation, so that depth and the frozen code together produce the residual. The construction has about 225K trainable parameters (225,114), small relative to the roughly 86M parameters of the frozen ViT backbone, so the adapter acts as a calibration on the prior rather than a re-training of it. Two properties of this construction matter for the rest of the pipeline. Because the output projection is zero-initialized, $\tilde z_u = z_u$ at the first optimizer step, so the adapter cannot degrade the frozen prior before any depth signal has been read. And because the DINOv2 backbone and CAUSE-TR head are kept frozen, the same $\tilde z_u$ remains usable by the k-means partition of §3.2 and by the feature-similarity cue used in SIMCF (§3.4).

Training the adapter calls for a self-supervised signal that pulls the adapted codes of pixels at similar depth toward each other while keeping them on the original CAUSE-TR manifold. DepthG [Sick et al., 2024] established that depth-feature correlation can guide unsupervised semantic learning when depth governs the loss, and we reuse the same loss at the adapter level rather than to the backbone weights. For each minibatch image, we sample 1024 pixels and 1024 pixel pairs, using the DepthG depth-similarity kernel with bandwidth $\sigma_d = 0.5$. The adapter minimizes $\mathcal{L}_\text{DCFA}=\mathcal{L}_\text{corr}+\lambda_\text{preserve}\mathcal{L}_\text{preserve}$, where $\mathcal{L}_\text{corr}$ is the DepthG correlation loss on $(\tilde z,D)$, $\mathcal{L}_\text{preserve}$ is the mean squared distance between $\tilde z$ and $z$, and $\lambda_\text{preserve}=20$. The correlation term encourages pixels on the same physical surface to remain clustered even when appearance changes, while the preservation term prevents collapse in regions where depth alone is uninformative. This contrasts with DepthG, which injects the depth-correlation signal into the backbone's training loop and modifies the ViT weights end-to-end. Freezing the backbone keeps the prior reusable across §3.2's clustering and §3.4's similarity cue, which is the property we exploit downstream.

Even with depth-conditioned semantic codes, the pseudo-label pair $(\hat S_x, \hat I_x)$ can still disagree at the output level. A depth fragment may straddle the hood-windshield seam of a car despite a coherent semantic region, and a pixel may take a depth value far from the per-class statistics of the class it has been assigned. SIMCF (§3.4) addresses these output-level disagreements through a label filter that requires three complementary cues: semantic identity, instance geometry, and feature similarity, to agree before a candidate label is accepted.
