---
section: "3.1 Problem Setup and Pipeline Overview"
status: locked v4 (2026-04-29) — drops θ/φ subscripts
paragraphs: 4
words: ~510
audit_history:
  - v1 specialized-agent draft
  - v2 reviewer corrections (six items)
  - v3 attribution softening + frozen-priors disclosure
  - v4 drops θ/φ subscripts on G and F (Eq. 1 now uses G(x) and F(x) directly)
equations: 1 (Eq. 1, input/output formal definition)
inviolable_structural_commitments:
  - Four paragraphs (¶1 setup + Eq. 1; ¶2 two-stage decomposition; ¶3 Figure 1 walkthrough; ¶4 roadmap)
  - Three explicit paragraph bridges ("monocular regime", "cues", "three modules")
  - Figure 1 referenced exactly once, in ¶3
  - Equation 1 is the ONLY equation in §3.1
  - Citations limited to Hahn 2025 (CUPS), Cordts 2016 (Cityscapes), Kim 2024 (CAUSE-TR)
  - Zero bold, ≤1 em-dash, word count [460, 540]
forbidden_in_section_3_1:
  - h=384, 16-D sinusoidal, 40K params, λ_preserve=20 (→ §3.3)
  - Sobel kernel sizes, τ_d, A_min, dilation iterations (→ §3.2 / §3.4)
  - τ_sim=0.85, SIMCF stage A/B/C math (→ §3.4)
  - 35.83, 32.76, 27.80, 25.4, +3.07 (→ §4)
---

# 3.1 Problem Setup and Pipeline Overview

We address scene-centric unsupervised panoptic segmentation in the monocular variant of the CUPS setting [Hahn et al., 2025], where a model must produce dense panoptic predictions without human annotations. Concretely, training is given an unlabeled image set $\mathcal{D}_{\text{train}} = \{x_i\}_{i=1}^{N}$, in which each frame $x_i \in \mathbb{R}^{H \times W \times 3}$ is a single RGB image drawn from the target dataset, here Cityscapes [Cordts et al., 2016]. The setting provides no semantic, instance, or panoptic annotations, no ground-truth or sensor-provided depth maps, no calibrated stereo views, and no temporal neighbours. We use only unlabeled target images for pseudo-label construction and panoptic network training, while relying on frozen off-the-shelf visual and depth priors. Formally, the two stages can be written as

$$G(x) = (\hat S_x, \hat I_x), \qquad F(x) = (S_x, I_x), \tag{1}$$

where $G$ is the monocular pseudo-label generator used during training and $F$ is the panoptic network evaluated at test time. The maps $\hat S_x$ and $S_x$ assign semantic labels to pixels, while $\hat I_x$ and $I_x$ assign instance identities to pixels belonging to countable object classes.

Working strictly within this monocular regime, we adopt the same two-stage decomposition that organizes CUPS. The first stage fits any pseudo-labeling components using only unlabeled target images, then applies the resulting generator independently to each frame, turning every $x_i$ into training targets $(\hat S_{x_i}, \hat I_{x_i})$. The second stage follows CUPS's panoptic bootstrapping and panoptic network training procedures on these pseudo-labels [Hahn et al., 2025]. Holding this training stage fixed lets us attribute changes in final network performance to the pseudo-label source, which is also the only place where stereo and motion cues entered the original CUPS recipe.

What replaces those cues is a Stage-1 generator built from three modules, illustrated in Figure 1. A semantic branch produces $\hat S_x$ by passing $x$ through a frozen DINOv2 backbone followed by the frozen CAUSE-TR head [Kim et al., 2024], whose semantic code is refined by the Depth-Conditioned Feature Adapter (DCFA) before being clustered into pseudo-classes. An instance branch produces $\hat I_x$ by computing Sobel responses on a monocular depth map and extracting connected components inside thing-eligible semantic regions. A cross-modal filter, SIMCF, then accepts, revises, or rejects candidate labels using three agreement cues: semantic identity, instance geometry, and feature similarity. The accepted pseudo-labels are passed to panoptic bootstrapping.

These three modules are developed in turn in the remainder of this section. Section 3.2 defines the monocular baseline that replaces CUPS's stereo and motion pseudo-labels with depth-gradient connected components while keeping the training procedure fixed. Section 3.3 introduces DCFA, which conditions the frozen semantic code on monocular depth through a residual feature-level injection. Section 3.4 specifies the SIMCF filter and the agreement criteria each cue contributes. Section 3.5 describes the CUPS-style panoptic bootstrapping and panoptic network training that consume the filtered pseudo-labels.
