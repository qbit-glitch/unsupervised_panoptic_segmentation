---
section: "3.4 SIMCF: Semantic Instance Mutual Consistency Filtering"
status: locked concise v5 (2026-04-29)
paragraphs: 2 + CLRS-style algorithm
words: ~170 prose + 7 algorithm lines
notes:
  - Renamed the filter to SIMCF.
  - Removed separate A/B/C subsections.
  - Kept the filter as one algorithm with three consistency checks.
  - Added CLRS-style line-numbered algorithm without bold fonts.
---

# 3.4 SIMCF: Semantic Instance Mutual Consistency Filtering

DCFA changes the feature space before clustering, but some errors remain after semantic and instance labels are formed. A depth fragment may cut through a single object, a proposal may contain pixels from multiple pseudo-classes, or a semantic assignment may sit at a depth far from the empirical range of its pseudo-class. SIMCF, short for Semantic Instance Mutual Consistency Filtering, is the final algorithm inside $G$: it takes the raw candidate pair $(S_x^0, I_x^0)$ and returns filtered pseudo-labels $(\hat S_x, \hat I_x)$ by enforcing consistency between semantic identity, instance geometry, feature similarity, and depth statistics.

Algorithm 1 lists the procedure. The first check prevents mixed-class instance targets, the second repairs depth over-fragmentation using DINOv3 appearance similarity, and the third converts depth-implausible pixels into void labels rather than hard negatives. SIMCF has no learned parameters; DCFA remains the only learned component inside the Stage-1 generator, and the filtered labels are passed directly to panoptic bootstrapping.

Algorithm 1. SIMCF$(S_x^0,I_x^0,D,\phi,v;\tau_\text{sim},\eta)$.

1. $(\hat S_x,\hat I_x) \gets (S_x^0,I_x^0)$
2. for each instance region $R_k=\{p:\hat I_x(p)=k\}$ do
3.     $c_k \gets \arg\max_c |\{p\in R_k:\phi(\hat S_x(p))=c\}|$
4.     $s_k \gets \arg\max_s |\{p\in R_k:\hat S_x(p)=s,\ \phi(s)=c_k\}|$
5.     for each $p\in R_k$ with $\phi(\hat S_x(p))\ne c_k$ do
6.         $\hat S_x(p)\gets s_k$
7. for each adjacent proposal pair $(a,b)$ in $\hat I_x$ do
8.     compute mean features $\bar v_a,\bar v_b$ over $R_a,R_b$
9.     if $c_a=c_b$ and $\cos(\bar v_a,\bar v_b)>\tau_\text{sim}$ then
10.        merge $R_a$ and $R_b$ in $\hat I_x$
11. for each pseudo-class $c$ do
12.    $P_c\gets\{p:\phi(\hat S_x(p))=c\}$
13.    $(\mu_c,\sigma_c)\gets(\operatorname{mean}D(P_c),\operatorname{std}D(P_c))$
14. for each pixel $p$ do
15.    $c\gets\phi(\hat S_x(p))$
16.    if $|D(p)-\mu_c|>\eta\sigma_c$ then
17.        $\hat S_x(p)\gets\mathrm{void}$; $\hat I_x(p)\gets0$
18. return $(\hat S_x,\hat I_x)$
